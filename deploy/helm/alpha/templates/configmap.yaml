apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "lago-alpha.fullname" . }}-env
  labels:
    {{- include "lago-alpha.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
