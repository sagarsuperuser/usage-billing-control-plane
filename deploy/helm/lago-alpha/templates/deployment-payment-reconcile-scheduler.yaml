{{- if .Values.paymentReconcileScheduler.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "lago-alpha.fullname" . }}-payment-reconcile-scheduler
  labels:
    {{- include "lago-alpha.labels" . | nindent 4 }}
    app.kubernetes.io/component: payment-reconcile-scheduler
spec:
  replicas: {{ .Values.paymentReconcileScheduler.replicas }}
  selector:
    matchLabels:
      {{- include "lago-alpha.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: payment-reconcile-scheduler
  template:
    metadata:
      labels:
        {{- include "lago-alpha.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: payment-reconcile-scheduler
        {{- with .Values.paymentReconcileScheduler.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.paymentReconcileScheduler.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      serviceAccountName: {{ include "lago-alpha.serviceAccountName.paymentReconcileScheduler" . }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: payment-reconcile-scheduler
          image: "{{ default .Values.api.image.repository .Values.paymentReconcileScheduler.image.repository }}:{{ default .Values.api.image.tag .Values.paymentReconcileScheduler.image.tag }}"
          imagePullPolicy: {{ .Values.paymentReconcileScheduler.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ include "lago-alpha.fullname" . }}-env
            - secretRef:
                name: {{ .Values.secretEnv.secretRefName }}
          env:
            - name: RUN_API_SERVER
              value: "false"
            - name: RUN_REPLAY_WORKER
              value: "false"
            - name: RUN_REPLAY_DISPATCHER
              value: "false"
            - name: RUN_BILLING_CONNECTION_CHECK_WORKER
              value: "false"
            - name: RUN_BILLING_CONNECTION_CHECK_SCHEDULER
              value: "false"
            - name: RUN_PAYMENT_RECONCILE_WORKER
              value: "false"
            - name: RUN_PAYMENT_RECONCILE_SCHEDULER
              value: "true"
            - name: RUN_DUNNING_WORKER
              value: "false"
            - name: RUN_DUNNING_SCHEDULER
              value: "false"
          resources:
            {{- toYaml .Values.paymentReconcileScheduler.resources | nindent 12 }}
{{- end }}
